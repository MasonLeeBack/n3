#include "stdafx.h"
#include "BsFxParser.h"
#include "bstreamext.h"

//------------------------------------------------------------------------------------------------

void BsFxParser::Parsing(char *szFxFileName)
{
	/*
	BFileStream Stream(szFxFileName);
	
	char buf[256];
	sprintf(buf, "%d\n", Stream.Length());
	OutputDebugString(buf);
	*/
}

//------------------------------------------------------------------------------------------------







